export type HomeRunEntry = {
  play_id: string;
  title: string;
  ExitVelocity: string;
  HitDistance: string;
  LaunchAngle: string;
  video: string;
};

export type User = {
  // id: number;
  user_name: string;
  email: string;
  info: string;
  elo: number;
};
